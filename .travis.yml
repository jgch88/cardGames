language: node_js

node_js:
  - 8

before_install:
- npm update
- export DISPLAY=:99.0
- sh -e /etc/init.d/xvfb start

install:
- npm install

script:
  npm run br && jest --silent

cache:
  directories:
    - "node_modules"
